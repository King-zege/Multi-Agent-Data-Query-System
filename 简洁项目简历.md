# 项目简历（简洁版）

## 一句话项目描述
基于LangGraph的多智能体系统，通过LLM意图识别动态路由用户的自然语言问题到专用智能体（SQL查询、数据分析、联网搜索），实现**自然语言→精准数据→深度洞察**。

## 项目背景
传统数据查询需要学习SQL或复杂报表工具。我们设计了智能系统：用户直接用自然语言提问，系统自动转SQL、执行、分析、可视化。

**版本演进**：v1.0基础架构 → v2.0长期记忆 → v2.1Web前端 → v3.0联网搜索

## 核心架构
```
问题 → 意图识别(6种) → 智能体路由 → 结果综合 → 答案

一主三从：MasterAgent + SQLQueryAgent + DataAnalysisAgent + WebSearchAgent
```

## 主要职责与成果

### 1. 多智能体架构（v1.0-v2.0）
- **Reflection自动纠错**：SQL失败→错误反馈LLM→自动修正（3次重试）
  - 首次成功率80% → 3次后98%+（显著降低用户交互成本）
- **意图识别+动态路由**：6种意图分类，准确率>95%
- **自动化数据分析**：统计→洞察→ECharts可视化（全自动）

### 2. 双层记忆系统（v2.0）
- **短期记忆**：对话历史自动压缩（20轮2000tokens→600tokens）
- **长期记忆**：SQLite存储用户偏好+知识库，后续会话注入上下文改进个性化

### 3. Web前端与流式API（v2.1）
- 7种SSE事件流：status/intent/sql/sources/chart/chunk/done
- Markdown渲染、ECharts可视化、实时打字机效果

### 4. 联网搜索整合（v3.0）
- Tavily搜索API集成，支持纯搜索和搜索+SQL联合对比
- 未配置时优雅降级，不影响其他功能

## 技术亮点

| 亮点 | 说明 |
|-----|------|
| **Reflection纠错** | LLM根据执行错误自动修正，降低交互成本 |
| **Few-shot工程** | 精心设计3-5个代表性示例，确保NL→SQL准确性 |
| **双层记忆** | 短期压缩+长期知识库，实现真正的跨会话学习 |
| **意图驱动架构** | 相比通用聊天机器人，专用智能体更清晰可控 |

## 定量成果

| 指标 | 数据 |
|-----|------|
| 意图识别准确率 | >95% |
| SQL首次成功率 | >80% |
| SQL最终成功率 | >98% |
| 平均查询耗时 | <500ms |
| 代码总量 | ~4000行 |

## 技术栈
**框架**：LangGraph + LangChain  
**LLM**：阿里通义千问(qwen-turbo)  
**搜索**：Tavily API  
**Web**：Flask + HTML/CSS/JS (ECharts/marked.js/highlight.js)  
**DB**：SQLite  

## 核心问题与解决

| 问题 | 解决方案 |
|-----|--------|
| SQL准确性低 | Reflection纠错 + Few-shot优化 |
| 长期记忆提取难 | 独立MemoryExtractor模块 + 置信度标记 |
| LLM输出格式不一致 | 统一_llm_to_str()方法 + 正则清理 |
| 意图识别边界模糊 | Few-shot定义边界 + 注入用户上下文 |

## 与其他方案对比

**vs 传统NL2SQL**：自动纠错（3次98%）vs 用户修改  
**vs 通用聊天机器人**：专用智能体+意图识别 vs 通用处理  
**vs 有状态聊天**：双层记忆（压缩+知识库）vs 简单历史保留  

## 面试核心问题

**Q：创新点？**  
A：不是技术新颖性，而是架构设计：意图驱动多智能体 + Reflection纠错 + 双层记忆的有机结合。

**Q：性能瓶颈？**  
A：LLM调用延迟（占比50-80%）。优化：缓存、并行调用。

**Q：有没有上线？**  
A：内部开发环境已部署。生产化需要认证、限流、监控等。

**Q：数据隐私？**  
A：本地SQLite存储，无上传云端（除prompt）；支持用户数据删除。

**Q：被质疑只是用现成框架？**  
A：框架只是工具。创新在设计：意图识别、Reflection纠错、双层记忆都是我们设计的（框架不提供）。Reflection为例，框架只提供LLM调用能力，但3次重试、错误处理、何时停止的逻辑都是我们的工程化。

## 总结

成功将LLM理解能力、编排能力、数据查询能力结合，解决"企业用户如何快速查询分析数据"的问题。

**核心创新**：Reflection纠错 + 双层记忆 + 意图驱动多智能体  
**展现能力**：架构设计 + 工程化 + 问题解决
