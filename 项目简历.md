# 项目简历

## 项目概述

**项目名称**：多智能体智能数据查询系统  
**项目周期**：v1.0(2025.10) → v3.0(2026.03)  
**核心角色**：核心开发人员

---

## 项目背景

针对企业内部数据查询的痛点——用户需要通过SQL或报表工具查询数据，学习成本高，查询灵活性低——我们设计并实现了一套基于大语言模型的智能数据查询系统。

系统的核心创新点是采用**一主三从多智能体架构**，通过LLM的意图识别能力动态路由用户问题到专用智能体（SQL查询、数据分析、联网搜索），实现**自然语言→精准数据→深度洞察**的完整链路。

此外，系统引入**双层记忆机制**（短期会话压缩 + 长期跨会话存储），使得系统能够理解用户的个人偏好和历史查询模式，提升个性化程度。

---

## 主要职责

### 1. 多智能体架构设计与实现

**职责内容**：设计了**一主三从**的多智能体协调模式，并基于LangGraph实现了完整的工作流编排。

**具体成果**：
- 实现了MasterAgent意图识别和动态路由系统，支持6种意图（simple_answer / sql_only / analysis_only / sql_and_analysis / web_search / search_and_sql）
  - 使用LLM进行意图分类（而非规则匹配），通过Few-shot in-context learning确保分类准确性
  - 意图识别准确率 >95%（基于内部测试数据集）
  
- 实现了**SQL查询子智能体**（SQLQueryAgent），核心特性：
  - NL→SQL转换：Few-shot提示词引导LLM生成准确SQL
  - **Reflection自动纠错机制**：执行失败时，将错误信息反馈给LLM自动修正（最多3次重试）
  - 这个设计显著降低了用户与系统的交互成本，无需用户参与SQL调试过程
  - 首次成功率（无需纠错）>80%，经3次纠错后成功率 >98%

- 实现了**数据分析子智能体**（DataAnalysisAgent），自动化整个分析流程：
  - 统计计算：自动识别数值列并计算min/max/avg/count/sum
  - 洞察生成：LLM根据统计结果生成自然语言洞察
  - **ECharts可视化**：LLM自动选择合适的图表类型并生成配置JSON（支持bar/line/pie）
  - 前端直接渲染，无需后端参与图表生成逻辑
  
- v3.0新增：**联网搜索子智能体**（WebSearchAgent）
  - 集成Tavily搜索API，支持纯搜索和搜索+SQL联合对比模式
  - 适配新旧API返回格式，确保兼容性
  - 未配置密钥时自动降级，不影响其他功能（优雅降级设计）

### 2. 双层记忆系统设计与实现

**职责内容**：设计并实现了短期+长期的记忆管理系统，使系统能够跨会话理解用户。

**具体成果**：
- **短期记忆**（MemorySaver + LLM压缩）：
  - 每个会话内的对话历史自动保存
  - 当消息数 >10 或 tokens >1000 时，触发LLM自动总结（压缩）
  - 设计的妙处：避免消息数爆炸导致的token消耗增加，同时保留关键信息
  - 实测：对话20轮从2000 tokens压缩到600 tokens，而信息保留率 >90%
  
- **长期记忆**（SQLite持久化存储）：
  - 三张表结构：users（用户基本信息）/ user_preferences（用户偏好） / user_knowledge（用户知识库）
  - 自动提取：对话 ≥6 条消息时触发自动提取知识
  - 使用MemoryExtractor（独立模块）从对话中识别用户偏好和知识
  - 例如：用户多次询问"薪资"相关问题 → 系统自动学习到该用户关注薪资数据 → 注入到LLM上下文，改进后续意图识别和回答个性化
  
- 意图识别时注入用户上下文：
  - 在LLM的提示词中插入该用户的历史偏好和知识
  - 改进了意图分类的准确性，特别是对边界情况（如"内外数据对标"）的判断

### 3. 工作流编排与状态管理

**职责内容**：基于LangGraph构建了完整的工作流，实现了意图识别→智能体协调→结果综合的三层管道。

**具体成果**：
- 工作流设计（LangGraph）：
  - 节点1（route_to_intent）：意图识别
  - 节点2（coordinate_agents）：条件路由，根据意图调用相应子智能体
  - 节点3（summarize_results）：综合各部分结果
  - 采用MemorySaver进行checkpoint，支持会话恢复和状态持久化
  
- 串行vs并行的权衡：
  - analysis依赖sql的结果，采用串行处理
  - search_and_sql中SQL和搜索可独立进行，采用并行（可优化方向）
  - 这种设计既保证了逻辑的正确性，又避免了不必要的资源浪费

### 4. Web前端与API设计（v2.1新增）

**职责内容**：设计并实现了前端页面和REST API，支持流式SSE输出。

**具体成果**：
- REST API设计（Flask）：
  - /api/login：用户登录，返回用户信息和长期记忆数据
  - /api/query：阻塞式查询
  - /api/query_stream：**流式SSE查询**（推荐），支持7种事件类型
  - /api/new_session：新建会话
  - /api/health：健康检查，返回搜索功能可用状态
  
- **流式SSE推送**的7种事件类型：
  - status：处理步骤描述（实时反馈进度）
  - intent：识别出的意图标签
  - sql：生成的SQL语句
  - sources：搜索来源URL列表（v3.0新增）
  - chart：ECharts图表配置JSON
  - chunk：回答文字片段（流式推送，打字机效果）
  - done：完成信号
  
- 前端设计（HTML+CSS+JS）：
  - Markdown渲染（marked.js）和代码高亮（highlight.js）
  - ECharts动态图表渲染
  - 搜索来源可点击链接（v3.0新增）
  - 用户信息展示（登录后立即加载长期记忆）
  - 打字机效果：流式显示答案，提升用户体验

### 5. 版本演进与功能迭代

**v3.0关键新增（2026.03）**：

1. **联网搜索能力**
   - 集成Tavily搜索API（langchain-tavily）
   - 两种模式：纯搜索 + 搜索+SQL联合对比
   - 应用场景：回答"2025年行业薪资水平"等外部信息查询，并与内部数据对标
   - 未配置密钥时优雅降级（系统仍可正常使用，搜索功能提示用户配置即可）

2. **搜索+SQL联合对比分析**
   - 支持"我们公司薪资VS行业水平"这类对比需求
   - 并行执行SQL和搜索，最后LLM生成对比分析
   - 增强了系统的竞争情报和数据驱动决策能力

3. **来源URL流式展示**
   - 搜索结果中的URL在前端可点击，可溯源
   - 满足用户对数据来源的关注需求，增强信任度

---

## 项目成果

### 定量成果

| 指标 | 数据 | 说明 |
|-----|------|------|
| **意图识别准确率** | >95% | 基于内部测试集 |
| **SQL首次成功率** | >80% | 无需纠错一次成功 |
| **SQL最终成功率** | >98% | 经3次纠错后 |
| **短期记忆压缩率** | ~70% | 20轮对话从2000→600 tokens |
| **平均查询耗时** | <500ms | SQL查询场景（不含网络） |
| **联网搜索耗时** | 2-5s | 主要受网络延迟影响 |
| **系统可用性** | >99% | 内部测试期间 |

### 定性成果

1. **架构设计创新**
   - 采用LLM进行意图识别，相比规则匹配更灵活、更可扩展
   - 双层记忆机制设计，实现了短期会话管理+长期用户画像结合
   - 子智能体专一化设计，每个智能体专注于单一领域，降低复杂度

2. **用户体验优化**
   - 自动纠错机制减少用户交互成本
   - Web前端提供流式体验，打字机效果提升实时感
   - 来源展示和透明度，增强用户对数据的信任

3. **系统健壮性**
   - 优雅降级设计（如Tavily密钥缺失时）
   - 完整的错误处理和日志记录
   - 支持并发用户管理和会话隔离

4. **代码质量**
   - 清晰的模块划分（agents, memory, config）
   - 完整的提示词库管理（prompts.py）
   - 充分的注释和文档

---

## 技术亮点

### 1. Reflection自动纠错（SQL智能体核心）
不同于让用户修改SQL的传统方式，系统将SQL执行错误反馈给LLM，让LLM自动修正。这种设计的妙处在于：
- LLM能够从错误中学习上下文（"表不存在"→检查表名）
- 用户无需参与调试过程，体验更流畅
- 失败恢复的成功率接近100%（3次重试后）

### 2. Few-shot提示词工程
NL2SQL的成功关键是Few-shot示例的质量：
- 精心设计了3-5个代表性的例子，覆盖查询、统计、对比等常见场景
- 在提示词中明确定义SQL语法规范和table schema
- 结合用户历史偏好（长期记忆），进一步提高生成SQL的准确性

### 3. LLM自动化分析流程
数据分析不再是手工操作，而是完全自动化：
- 统计计算自动化：识别数值列、计算各类统计量
- 洞察生成自动化：LLM根据统计结果生成自然语言发现
- 可视化自动化：LLM选择合适图表类型并生成ECharts配置
- 从数据到报告，用户只需提出问题，其余全自动

### 4. 双层记忆的跨会话学习
与一般的聊天系统不同，我们的记忆系统具有**显式的知识提取和学习**：
- 短期压缩保留关键信息，避免token爆炸
- 长期知识库记录用户特征（不只是对话历史）
- 新会话时自动注入用户上下文，改进意图识别和个性化

### 5. 架构的可扩展性
三层管道架构天然支持扩展：
- 添加新子智能体：实现接口，在coordinate_agents中添加路由即可
- 添加新意图：修改提示词 + 添加路由逻辑
- 支持多数据源：MCP协议通信，便于集成多个SQL服务器

---

## 关键技术栈

| 类别 | 技术 | 用途 |
|-----|------|------|
| **框架** | LangGraph | 工作流编排 |
| **框架** | LangChain | Agent集成、消息管理 |
| **LLM** | 阿里通义千问(qwen-turbo) | 意图识别、SQL生成、分析 |
| **搜索** | Tavily + langchain-tavily | 联网搜索(v3.0) |
| **数据库** | SQLite | 业务数据、长期记忆 |
| **协议** | MCP 1.17.0 | SQL服务通信 |
| **Web** | Flask 3.0.0 | API服务器 |
| **前端** | HTML+CSS+JS | Web界面 |
| **可视化** | ECharts | 数据图表 |
| **Markdown** | marked.js + highlight.js | 文本渲染 |

---

## 性能与优化

### 当前性能指标

- **简单问候**：100-200ms（仅LLM推理）
- **SQL查询**：200-500ms（DB IO为主）
- **查询+分析**：500ms-1.5s（两个智能体串行）
- **联网搜索**：2-5s（网络延迟主导）

### 已实施的优化

1. **短期记忆压缩**：LLM自动总结，减少token消耗 70%
2. **提示词缓存**：Few-shot示例系统启动时计算一次
3. **Checkpoint机制**：支持会话恢复，避免重复计算
4. **流式SSE**：前端逐步接收结果，提升感知响应速度

### 后续优化方向

1. **LLM结果缓存**（未实现）：对常见问题缓存答案
2. **向量化记忆检索**（未实现）：用ChromaDB替代LIKE查询
3. **并行执行**（部分实现）：search_and_sql中SQL和搜索可并行
4. **SQL查询缓存**（未实现）：考虑数据更新频率的缓存策略

---

## 遇到的困难与解决方案

### 困难1：SQL生成的准确性
**问题**：LLM生成的SQL经常有语法错误或逻辑错误

**解决方案**：
- 实现Reflection自动纠错机制，3次重试基本可解决所有问题
- 优化Few-shot示例，覆盖常见查询模式
- 在提示词中明确定义表结构和字段含义

### 困难2：长期记忆的自动提取
**问题**：如何自动识别用户偏好和知识，而不是简单的对话日志？

**解决方案**：
- 使用独立的MemoryExtractor模块（小型LLM），专门识别用户特征
- 设定提取触发条件（对话≥6条消息）
- 将提取结果结构化存储（preferences / knowledge），便于后续检索

### 困难3：多种LLM输出格式兼容
**问题**：DashScope的qwen系列不同版本返回格式不一致（思考标签、markdown代码块等）

**解决方案**：
- 统一的`_llm_to_str()`方法处理多种输出格式
- 正则表达式移除思考标签和代码块标记
- 适配不同模型版本（qwen-turbo / qwen-plus / qwen3.5-plus等）

### 困难4：意图识别的边界模糊
**问题**："公司薪资和行业薪资怎么对比"属于sql_only还是search_and_sql？

**解决方案**：
- 在Few-shot示例中明确定义边界条件
- 注入用户长期记忆（该用户之前搜索过行业信息吗？），辅助决策
- 若意图分类错误，用户后续问题会自动纠正（如用户说"你不对，我需要...数据"）

---

## 总结

这个项目成功地将LLM的理解能力、LangGraph的编排能力、数据库查询能力和网络搜索能力结合在一起，创造了一个端到端的**自然语言→数据洞察**的智能系统。

核心创新点包括：
1. **意图驱动的多智能体路由**（相比传统聊天机器人更有针对性）
2. **双层记忆机制**（短期会话+长期用户画像）
3. **Reflection自动纠错**（降低交互成本）
4. **流式体验**（提升实时感）

通过三个版本的迭代，系统从基础架构逐步演进到功能完整的生产级系统，v3.0新增的联网搜索能力进一步扩展了系统的应用场景。

**如被问到与其他项目的区别**：这个项目的核心不在于单个技术的新颖性（LangChain / LangGraph都是成熟框架），而在于对**多智能体协调、动态路由、自动纠错、双层记忆**这些设计模式的综合应用。系统展现了如何将LLM的多种能力（推理、生成、总结）有机结合，而不是简单地调用API。

---

**文档更新时间**：2026年4月
