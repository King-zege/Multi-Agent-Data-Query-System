# 怎么跑这个项目

## 环境配置

### 系统要求

| 要求 | 版本 | 说明 |
|-----|------|------|
| **Python** | 3.8+ | 建议使用3.10+ |
| **操作系统** | Windows/Linux/Mac | 跨平台支持 |
| **磁盘空间** | 500MB+ | 依赖包+数据库 |
| **内存** | 2GB+ | 建议4GB以上 |
| **网络** | 必需 | DashScope API、Tavily搜索(可选) |

### 依赖包列表

```
# 核心依赖
langchain>=1.0.0
langchain-core>=1.0.0
langchain-community>=0.3.0
langchain-openai>=1.0.0
langgraph>=1.0.0
langgraph-checkpoint>=3.0.0

# LLM & 搜索
dashscope>=1.24.0
langchain-tavily

# 工具
mcp==1.17.0
pyyaml==6.0.3
rich==14.2.0
flask==3.0.0
flask-cors==4.0.0

# 数据库
sqlite3 (内置)
```

### 环境变量说明

#### 必需环境变量

**DASHSCOPE_API_KEY**
```bash
# 申请阿里云账户 → 开通DashScope服务 → 生成API密钥

# Linux/Mac 设置
export DASHSCOPE_API_KEY=sk_xxxxxxxxxxxxx

# Windows PowerShell 设置
$env:DASHSCOPE_API_KEY = "sk_xxxxxxxxxxxxx"

# Windows CMD 设置
set DASHSCOPE_API_KEY=sk_xxxxxxxxxxxxx

# 验证
echo $DASHSCOPE_API_KEY  # 应输出密钥内容
```

#### 可选环境变量

**TAVILY_API_KEY** (联网搜索功能)
```bash
# 免费套餐每月1000次请求

export TAVILY_API_KEY=tvly_xxxxxxxxxxxxx

# 未设置时，搜索功能自动禁用，不影响其他功能
```

### 配置文件示例

编辑 `config/config.yaml`：

```yaml
llm:
  provider: "dashscope"
  model: "qwen-turbo"              # 可选：qwen-plus-latest / qwen-max-latest / qwen3.5-plus
  api_key: "${DASHSCOPE_API_KEY}"  # 从环境变量读取
  base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  temperature: 0.1                 # 0-1, 越低越确定
  max_tokens: 2048
  streaming: true

database:
  path: "./data/company.db"

nl2sql:
  num_examples: 3                  # Few-shot示例数
  max_retries: 3                   # 纠错最多重试次数

memory:
  long_term_db: "./data/long_term_memory.db"
  short_term_max_tokens: 1000
  compression_threshold: 10        # 消息数>10或tokens>1000时压缩
  auto_extract_knowledge: true

search:
  tavily_api_key: ""               # 留空则从环境变量${TAVILY_API_KEY}读取
  max_results: 5
  search_depth: "advanced"

web:
  host: "0.0.0.0"
  port: 5000
  debug: false
  cors_origins: "*"
```

---

## 运行步骤

### 步骤1：项目克隆与环境准备

```bash
# 1.1 进入项目目录
cd Multi-Agent-Exp

# 1.2 创建虚拟环境（推荐）
python -m venv venv

# 1.3 激活虚拟环境
# Linux/Mac
source venv/bin/activate

# Windows
venv\Scripts\activate

# 1.4 验证激活成功
python --version  # 应显示虚拟环境中的Python版本
which python      # Linux/Mac，应显示虚拟环境路径
```

### 步骤2：安装依赖

```bash
# 2.1 升级pip
pip install --upgrade pip

# 2.2 安装所有依赖
pip install -r requirements.txt

# 2.3 验证安装
python -c "import langchain; import dashscope; print('✓ 依赖安装成功')"
```

### 步骤3：设置API密钥

```bash
# 3.1 设置必需的DashScope密钥

# Linux/Mac (永久设置，建议加入 ~/.bashrc)
export DASHSCOPE_API_KEY=sk_xxxxxxxxxxxxx

# Windows PowerShell (当前会话)
$env:DASHSCOPE_API_KEY = "sk_xxxxxxxxxxxxx"

# Windows CMD (当前会话)
set DASHSCOPE_API_KEY=sk_xxxxxxxxxxxxx

# 3.2 验证密钥设置
echo $DASHSCOPE_API_KEY  # 应输出密钥

# 3.3 (可选) 设置Tavily搜索密钥
export TAVILY_API_KEY=tvly_xxxxxxxxxxxxx
```

### 步骤4：初始化数据库

```bash
# 4.1 初始化业务数据库（员工、部门数据）
python data/init_db.py

# 输出应显示：
# ✓ 已创建 employees 表
# ✓ 已创建 departments 表
# ✓ 插入示例数据完成

# 4.2 初始化长期记忆数据库（用户偏好、知识库）
python data/init_memory_db.py

# 输出应显示：
# ✓ 已创建 users 表
# ✓ 已创建 user_preferences 表
# ✓ 已创建 user_knowledge 表

# 4.3 验证数据库初始化
ls -la data/  # 应显示 company.db 和 long_term_memory.db
```

### 步骤5：启动项目

#### 方式1：Web前端（推荐）⭐

```bash
# 自动执行：环境检查 → 数据库初始化 → 启动Flask服务 → 打开浏览器

# Linux/Mac
chmod +x start_web.sh
./start_web.sh

# Windows
start_web.bat

# 或手动启动
python app.py

# 输出应显示：
# * Running on http://0.0.0.0:5000
# * Press CTRL+C to quit

# 浏览器自动打开：http://localhost:5000
# 如未打开，手动访问：http://localhost:5000
```

#### 方式2：命令行模式

```bash
# 交互式命令行
python agent.py

# 输出示例：
# ╭────────────────────────────────────────────────────╮
# │ LangGraph 多智能体数据查询系统 v3.0                 │
# │ 主智能体 + SQL查询 + 数据分析 + Web前端             │
# │ 智能路由 · 深度分析 · 长短期记忆                   │
# ╰────────────────────────────────────────────────────╯
# 
# 请输入用户ID（用于保存您的偏好和记忆）：guest
# 
# 欢迎 guest！系统已就绪
# 会话ID: sess_abc123...
# 
# 特殊命令：
#   - 输入 'new' 开始新会话（清空短期记忆）
#   - 输入 'info' 查看用户信息
#   - 输入 'exit' 或 'quit' 退出系统
#
# 请输入问题：
```

#### 方式3：API调用

```python
# Python脚本调用
from agent import MultiAgentSystem

# 初始化系统
system = MultiAgentSystem(config_path="config/config.yaml")

# 用户登录
system.login("user123")

# 执行查询
answer = system.query("公司有多少名员工？")
print(answer)

# 流式查询
for event in system.stream_query("分析各部门的薪资分布"):
    print(event)
```

---

## 运行时参数说明

### CLI模式特殊命令

| 命令 | 说明 | 示例 |
|-----|------|------|
| `new` | 开始新会话，清空短期记忆，保留长期记忆 | > new |
| `info` | 查看当前用户信息和偏好 | > info |
| `exit` / `quit` / `q` | 退出系统 | > exit |

### Web界面参数

**登录**：输入user_id（字符串，无长度限制）

**查询**：支持Markdown、代码块等富文本格式

**会话管理**：页面刷新后session_id不变，保持连接

---

## 数据查看

### 方式1：Web界面查看（推荐）

1. **访问前端**：http://localhost:5000
2. **登录**：输入user_id (如: user_001)
3. **查看偏好**：登录后界面自动加载
4. **提问查询**：直接在聊天框输入问题

### 方式2：数据库直接查看

```bash
# 2.1 查看业务数据库
sqlite3 data/company.db

# 查看所有表
sqlite> .tables
# 输出：departments  employees

# 查看employees表结构
sqlite> .schema employees

# 查询员工信息
sqlite> SELECT * FROM employees LIMIT 5;
sqlite> SELECT department, COUNT(*), AVG(salary) FROM employees GROUP BY department;

# 查看departments表
sqlite> SELECT * FROM departments;

# 2.2 查看长期记忆数据库
sqlite3 data/long_term_memory.db

# 查看所有用户
sqlite> SELECT * FROM users;

# 查看特定用户的偏好
sqlite> SELECT * FROM user_preferences WHERE user_id='user123';

# 查看特定用户的知识库
sqlite> SELECT * FROM user_knowledge WHERE user_id='user123' LIMIT 10;

# 2.3 退出sqlite
sqlite> .quit
```

### 方式3：日志和调试信息

```bash
# 启用debug模式（修改config.yaml）
web:
  debug: true

# 运行时查看详细日志
export PYTHONUNBUFFERED=1
python app.py

# 查看特定模块的日志
export LOGLEVEL=DEBUG
python app.py
```

### 方式4：查看数据的常用SQL

```sql
-- 查询某部门的薪资统计
SELECT 
    department,
    COUNT(*) as '员工数',
    AVG(salary) as '平均薪资',
    MIN(salary) as '最低薪资',
    MAX(salary) as '最高薪资'
FROM employees
GROUP BY department
ORDER BY AVG(salary) DESC;

-- 查询薪资最高的10名员工
SELECT name, department, position, salary
FROM employees
ORDER BY salary DESC
LIMIT 10;

-- 查询各城市的部门分布
SELECT city, COUNT(DISTINCT name) as '部门数'
FROM departments
GROUP BY city;

-- 查询用户查询历史（通过知识库）
SELECT * FROM user_knowledge
WHERE user_id='user123'
ORDER BY created_at DESC;
```

---

## 典型运行流程（图示）

```
【启动流程】

1. 用户执行启动命令
   └─> ./start_web.sh / python app.py

2. 系统初始化
   ├─> 加载config.yaml
   ├─> 连接DashScope API
   ├─> 检查data/company.db存在性
   ├─> 检查data/long_term_memory.db存在性
   └─> 启动Flask服务

3. Flask服务启动
   ├─> 监听 http://0.0.0.0:5000
   ├─> 加载static/文件夹（前端）
   └─> 等待连接

4. 用户访问浏览器
   └─> http://localhost:5000

5. 前端加载
   ├─> 加载static/index.html
   ├─> 加载static/style.css（样式）
   ├─> 加载static/app.js（逻辑）
   └─> 显示登录界面

【查询流程】

1. 用户输入user_id并点击登录
   └─> POST /api/login {user_id: "user123"}

2. 后端处理登录
   ├─> MultiAgentSystem.login()
   ├─> 读取长期记忆数据
   └─> 返回用户信息和知识库

3. 前端显示用户信息
   └─> 展示偏好、知识库等

4. 用户输入问题并发送
   └─> POST /api/query_stream {question: "..."}

5. 后端处理查询（流式）
   ├─> MasterAgent.stream_query()
   ├─> 逐步执行工作流
   ├─> 生成SSE事件流
   │   ├─ status: "正在识别意图..."
   │   ├─ intent: "sql_and_analysis"
   │   ├─ sql: "SELECT..."
   │   ├─ chart: {type: 'bar', ...}
   │   ├─ chunk: "根据..."
   │   └─ done: "完成"
   └─> 返回SSE流

6. 前端实时接收和渲染
   ├─> 更新status状态栏
   ├─> 显示intent标签
   ├─> 显示SQL语句
   ├─> 渲染ECharts图表
   ├─> 逐字流式显示答案
   └─> 显示完成信号

7. 用户查看结果
   └─> 可查看答案、图表、来源等

8. 用户继续提问或退出
   └─> 回到步骤4或关闭页面
```

---

## 常见问题排查

### Q1：运行 `python agent.py` 报错 `ModuleNotFoundError: No module named 'dashscope'`

**检查清单**：
```bash
# ✓ 虚拟环境已激活
which python  # 应显示虚拟环境路径

# ✓ 依赖已安装
pip list | grep dashscope

# ✓ 重新安装
pip install -r requirements.txt --force-reinstall

# ✓ 验证
python -c "import dashscope; print('OK')"
```

### Q2：报错 `DASHSCOPE_API_KEY environment variable not found` 或 `API Key is invalid`

**检查清单**：
```bash
# ✓ 密钥已设置
echo $DASHSCOPE_API_KEY

# ✓ 密钥格式正确
# 应该以 sk_ 开头

# ✓ 密钥未过期
# 请检查密钥是否过期

# ✓ 重新设置
export DASHSCOPE_API_KEY=sk_xxxxxxxxxxxxx

# ✓ 重新运行
python agent.py
```

### Q3：运行 `python data/init_db.py` 报错 `PermissionError: [Errno 13] Permission denied`

**检查清单**：
```bash
# ✓ data文件夹存在且可写
ls -la data/

# ✓ 删除旧数据库
rm -f data/company.db

# ✓ 重新初始化
python data/init_db.py

# ✓ 如果问题持续，检查路径
pwd  # 确认当前目录
```

### Q4：Web访问 http://localhost:5000 显示404或无响应

**检查清单**：
```bash
# ✓ Flask服务已启动
# 终端应显示 "* Running on http://0.0.0.0:5000"

# ✓ 端口未被占用
# Windows: netstat -ano | find "5000"
# Linux/Mac: lsof -i :5000

# ✓ 防火墙未阻止
# 允许localhost:5000的访问

# ✓ 前端文件存在
ls -la static/index.html

# ✓ 重启Flask
# Ctrl+C 停止
# python app.py 重新启动
```

### Q5：查询返回 `[Error] no such table: employees` 或类似错误

**检查清单**：
```bash
# ✓ 数据库已初始化
python data/init_db.py

# ✓ 检查表是否存在
sqlite3 data/company.db ".tables"

# ✓ 如果表不存在，重新初始化
rm -f data/company.db
python data/init_db.py

# ✓ 检查数据库路径在config.yaml中正确
cat config/config.yaml | grep "path:"
```

### Q6：搜索功能无法使用，报 "搜索功能未配置"

**检查清单**：
```bash
# ✓ Tavily密钥已设置（可选）
export TAVILY_API_KEY=tvly_xxxxxxxxxxxxx

# ✓ 或在config.yaml中设置
# search:
#   tavily_api_key: "tvly_xxxxxxxxxxxxx"

# ✓ 验证/api/health
curl http://localhost:5000/api/health
# 应返回 {"web_search": true}

# ✓ 未配置时正常使用其他功能
# 搜索功能自动禁用
```

---

## 典型提问及预期输出

### 测试1：简单问候

**输入**：`你好，你能帮我做什么？`

**预期输出**：
```
意图：simple_answer
回答：你好！我是一个智能数据查询助手，我可以帮你...
```

### 测试2：纯SQL查询

**输入**：`公司总共有多少个部门？`

**预期输出**：
```
意图：sql_only
SQL：SELECT COUNT(DISTINCT name) FROM departments
结果：5个部门（研发部、产品部、设计部、运营部、市场部）
```

### 测试3：查询+分析

**输入**：`对比一下研发部、产品部和设计部的平均薪资`

**预期输出**：
```
意图：sql_and_analysis
SQL：SELECT department, AVG(salary) FROM employees WHERE department IN (...) GROUP BY department
图表：ECharts柱状图，展示三个部门的薪资对比
分析：
- 研发部平均薪资最高：55000元
- 产品部次之：48000元  
- 设计部最低：42000元
- 研发部比设计部高13000元（31%）
```

### 测试4：联网搜索（需配置TAVILY_API_KEY）

**输入**：`2025年互联网行业软件工程师的平均薪资是多少？`

**预期输出**：
```
意图：web_search
来源：[报告1的URL], [报告2的URL], ...
答案：根据最新行业报告，2025年互联网行业软件工程师平均薪资为...
```

---

## 性能基准

| 场景 | 耗时 | 备注 |
|-----|------|------|
| **简单问候** | 100-200ms | 仅LLM调用一次 |
| **SQL查询** (无纠错) | 200-500ms | 涉及数据库IO |
| **查询+分析** | 500ms-1.5s | 两个子智能体串行 |
| **联网搜索** | 2-5s | 网络延迟主导 |
| **搜索+SQL对比** | 5-10s | 并行调用但网络延迟 |

---

**文档更新时间**：2026年4月  
**适用版本**：v3.0+
